# Dependencias
/node_modules

# Next.js
/.next
/out
/build
next-env.d.ts

# TypeScript
*.tsbuildinfo

# Historial local (modo USE_LOCAL_STORAGE). En Vercel se usa Postgres, no el disco.
/historial

# Variables de entorno — NUNCA subir claves (ANTHROPIC_API_KEY, POSTGRES_URL…).
# .env.example SÍ se versiona (es la plantilla sin secretos).
.env
.env.*
!.env.example

# Sistema / logs
.DS_Store
Thumbs.db
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
